document.addEventListener('DOMContentLoaded',()=>{const target=document.querySelector('#detailContent');if(!target)return;const id=Number(new URLSearchParams(location.search).get('id'))||1;const d=window.DORM_DATA.find(item=>item.id===id)||window.DORM_DATA[0];document.title=`${d.name} | HAU Dorm Finder`;target.innerHTML=`<div class="detail-visual" role="img" aria-label="Illustration of ${d.name}"></div><div class="container"><div class="detail-layout"><main class="detail-main"><h1>${d.name}</h1><p class="price">₱${d.price.toLocaleString()} <small>/ month</small></p><h2>About this dorm</h2><p>${d.description}</p><h2>Amenities</h2><div class="amenity-tags">${d.amenities.map(a=>`<span class="tag">${a}</span>`).join('')}</div><div class="detail-actions"><a class="btn btn-primary" href="contact.html">Contact Owner</a><a class="btn btn-secondary" href="listings.html">← Back to Listings</a></div></main><aside class="info-card"><h2>Quick information</h2><div class="info-item"><span class="info-label">Full address</span><span class="info-value">${d.address}</span></div><div class="info-item"><span class="info-label">Distance from HAU</span><span class="info-value">${d.distance}</span></div><div class="info-item"><span class="info-label">Contact number</span><span class="info-value">${d.contactNumber}</span></div><div class="info-item"><span class="info-label">Listing status</span><span class="info-value">Verified student listing</span></div></aside></div></div>`;});
